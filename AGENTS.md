# AGENTS

## Project Defaults
- If you ask to change a value "по умолчанию" or "поменять", update CSS default variables in `app/globals.css`.

## Branching / Worktrees
- Work only on `main`.
- Do not create or use additional git worktrees.

## Skills
- Store skills only in `~/.codex/skills` (global) and do not keep skill files or testing notes in this repo.
